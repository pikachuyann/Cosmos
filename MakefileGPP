export CC=g++
export OPTIONS=-std=gnu++0x

all: classes Generator 
	
classes:
	make -C SOURCES/Cosmos classes

Generator:
	make -C SOURCES/ModelGenerator Generator


clean:
	make -C SOURCES/Cosmos clean
	make -C SOURCES/ModelGenerator clean