
NbVariables = 1;
NbLocations = 3;
const int N=2;

VariablesList = {x} ;

LocationsList = {li, lp ,lm};
AVG(Last(x));
InitialLocations={li};

FinalLocations={ lp, lm };

Locations={
(li, TRUE );
(lp, (RE_nPlace1 + RE_nPlace4 = N) & (RE_nPlace1 <  RE_nPlace4) & (Puit = 0));
(lm, (RE_nPlace1 + RE_nPlace4 = N) & (RE_nPlace1 >= RE_nPlace4) | (Puit > 0));
}; 

Edges={
((li,li)  ,ALL ,  # , #);
((li,lp)  ,# ,  # , {x=1});
((li,lm)  ,# ,  # , #);
};